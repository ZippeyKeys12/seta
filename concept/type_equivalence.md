# Type Equivalences

## Basic Types

`T?` <=> `T | Null`

<!-- `T?` <=> `Maybe[T]` -->

`~Anything` <=> `Nothing`

`~Nothing` <=> `Anything`

<!-- ## Collections

`T[]` <=> `Array<T>`

`[T]` <=> `List<T>`

`{T}` <=> `Set<T>`

`{T: V}` <=> `Map<T, V>` -->
